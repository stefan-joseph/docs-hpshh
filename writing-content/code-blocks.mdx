`Reiciendis` repellendus beatae. Et natus sit voluptatem pariatur pariatur non. At possimus ea sit debitis et nam. Quia culpa tempore ex. Quam nam non sunt commodi illo aliquid est. Harum amet tempora odio voluptatem deserunt.

<CodeGroup>
  ```typescript title="file-name.ts"
  console.log("Hello from TypeScript");
  ```

```python title="file-name.py"
print("Hello from Python")
```

</CodeGroup>

```typescript title="file-name.ts"
console.log("Hello from TypeScript");
```

```tsx title="file-name.ts"
function Item({ name, isPacked }) {
  if (isPacked) {
    return null;
  }
  return <li className="item">{name}</li>;
}

export default function PackingList() {
  return (
    <section>
      <h1>Sally Ride's Packing List</h1>
      <ul>
        <Item isPacked={true} name="Space suit" />
        <Item isPacked={true} name="Helmet with a golden leaf" />
        <Item isPacked={false} name="Photo of Tam" />
      </ul>
    </section>
  );
}
```

```plaintext {1} title="file-name.ts" showLineNumbers
  console.log("Hello from TypeScript");
```

<CodeGroup>
  ```plaintext title="file-name.ts" showLineNumbers
  console.log("Hello from TypeScript");
  ```

```python title="file-name.py"
print("Hello from Python")
```

```python title="file-name-3.py"

```

```python title="file-name-4.py"

```

</CodeGroup>

<CodeGroup>
  ```plaintext title="file-name.ts"
  console.log("Hello from TypeScript");
  ```

```javascript title="file-name.js"
console.log("Hello from JavaScript");
```

</CodeGroup>

<CodeGroup>
  ```javascript {1} title="file-name.ts"
  console.log("Hello from TypeScript");
  ```

```javascript title="file-name.js"
console.log("Hello from JavaScript");
```

</CodeGroup>

```plaintext title="file-name.txt"

```

<CodeGroup>
  ```javascript title="snippet.js"
  console.log("Hello from JavaScript");
  ```

```javascript {2,4-6} title="njknkjnjknsnippet.js"
import fs from "node:fs";

export default async function Page() {
  // Synchronous file system read
  const content = fs.readFileSync("./config.json", "utf-8");

  // Module imports
  const constants = await import("./constants.json");

  // Pure computations
  const processed = JSON.parse(content).items.map((item) => item.value \* 2);

  return (
    <div>
      <h1>{constants.appName}</h1>
      <ul>
        {processed.map((value, i) => (
          <li key={i}>{value}</li>
        ))}
      </ul>
    </div>
  );
}
```

```javascript title="snippet-3.js"

```

```javascript title="snippet-4.js"

```

```javascript title="snippet-5.js"

```

```javascript title="snippet-6.js"

```

```javascript title="snippet-7.js"

```

</CodeGroup>

<CodeGroup>
  ```typescript title="snippet.ts"
  console.log("Hello from TypeScript");
  ```

```javascript title="snippet.js"
console.log("Hello from JavaScript");
```

</CodeGroup>

<CodeGroup>
  ```javascript collapse={1-2,4-8} {2,4-6,22-23} title="snippet.js" showLineNumbers
  import fs from "node:fs";

export default async function Page() {
// Synchronous file system read
const content = fs.readFileSync("./config.json", "utf-8");

    // Module imports
    const constants = await import("./constants.json");

    // Pure computations
    const processed = JSON.parse(content).items.map((item) => item.value \* 2);

    return (
      <div>
        <h1>{constants.appName}</h1>
        <ul>
          {processed.map((value, i) => (
            <li key={i}>{value}</li>
          ))}
        </ul>
      </div>
    );

}

````

```javascript title="snippet-2.js"
````

</CodeGroup>

```javascript
console.log("hello");
const number = 12;
```

Ut qui facilis quia delectus et nesciunt qui ut vitae. Eum non officia exercitationem perferendis asperiores adipisci consectetur adipisci. Quae deserunt qui quisquam non et cum aut.

```ts
type Pet = "cat" | "dog" | "hamster";

function adoptPet(name: string, type: Pet) {
  return `${name} the ${type} is now adopted!`;
}

// Hover to see the inferred types
const message = adoptPet("Mintie", "cat");
```

Aut qui enim accusamus eos. Quas laboriosam voluptas a natus assumenda iusto soluta ex omnis. Iste consectetur deleniti quos odio doloremque et voluptate. Totam iusto eum ut.

```bash
echo "This terminal frame has no title"
```

```shell
echo "This terminal frame has no title"
```

```js
// Example with wrap
function getLongString() {
  return "This is a very long string that will most probably not fit into the available space unless the container is extremely wide";
}
```

```js
// Example with wrap=false
function getLongString() {
  return "This is a very long string that will most probably not fit into the available space unless the container is extremely wide";
}
```
